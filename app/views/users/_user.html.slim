.column
  .card
    .card-content
      .media
        .media-left
          = display_user_icon(user)
        .media-content
          .subtitle.is-6.has-text-weight-bold.is-marginless
            = link_to user.name, user_path(user)
            span.tag.is-success.is-pulled-right
              = Schedule.where(user_id: user.id).sum(:study_time)
              | 分積み上げ
